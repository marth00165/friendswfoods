<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :user_event_id %>
    <%= form.number_field :user_event_id %>
  </div>

  <div class="field">
    <%= form.label :restaurant_event_id %>
    <%= form.number_field :restaurant_event_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
